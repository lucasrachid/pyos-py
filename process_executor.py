import subprocess
from execute_class import execute_class

class process_executor:

    def do_process(self, class_to_execute, process):
        print(class_to_execute)
        print(process)
        pass